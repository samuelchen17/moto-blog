import React from "react";
import IPageProps from "../interfaces/page.interface";

const RegisterPage = (props: IPageProps) => {
  return <div className="min-h-screen">Register page</div>;
};

export default RegisterPage;
