{
  "name": "server",
  "version": "1.0.0",
  "description": "blog",
  "main": "src/server.ts",
  "scripts": {
    "build": "rm -rf build && prettier --write src/ && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.5.1"
  }
}
